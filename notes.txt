Project Setup

Adding and Removing Dependencies
for EF Core projects: 
Go to the root directory of the production project (ToDoList) via the terminal and add the following dependencies.

$ dotnet add package Microsoft.EntityFrameworkCore -v 6.0.0
$ dotnet add package Pomelo.EntityFrameworkCore.MySql -v 6.0.0

Check out Entity Framework Core Official GitHub Repo to learn more.